{
  "permissions": {
    "allow": [
      "Bash(npx eslint *)",
      "Bash(timeout 40 bash -c 'until curl -sf http://localhost:3000 >/dev/null 2>&1; do sleep 1; done')"
    ]
  },
  "enabledPlugins": {
    "agent-sdk-dev@claude-plugins-official": true
  }
}
